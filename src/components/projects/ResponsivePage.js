import React from 'react';
import ResponsiveComponent from './ResponsiveComponent';

export default function ResponsivePage({ history }) {
  return (
    <div>
      <ResponsiveComponent history={history} />
    </div>
  );
}
